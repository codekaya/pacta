# Hangi anchor? Üretim anchor'ına geçerken yalnızca bu iki satır değişir (PRD FR-21).
STELLAR_NETWORK=testnet
ANCHOR_HOME_DOMAIN=tr-mock-anchor.fly.dev
ANCHOR_ASSET_CODE=USDC

# Klinik cüzdanı. Boşsa testnet'te anchor/.wallet.json üretilir.
CLINIC_SECRET=

# Kliniğin TL ödeme IBAN'ı (SEP-12 bank_account_number). Boşsa anchor sandbox IBAN'ı kullanır.
CLINIC_IBAN=
